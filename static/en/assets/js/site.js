// var smartLoadImages = function() {
//   [].slice.call(document.querySelectorAll('[data-src]')).forEach(function(el) {
//     if (el.parentNode.offsetHeight && (el.getAttribute("src") === "img/_p.gif"))
//       el.setAttribute("src", el.getAttribute("data-src"));
//   });


// };

// document.addEventListener("DOMContentLoaded", smartLoadImages, false);
// window.addEventListener("resize", smartLoadImages, false); // Needs debounce